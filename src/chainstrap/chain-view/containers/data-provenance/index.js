import React, { Component } from 'react'
import { connect } from 'react-redux'
import PropTypes from 'prop-types'

class DataProvenance extends Component {
  render() {
    return <div>WIP</div>
  }
}

export default connect()(DataProvenance)
